import { StyleSheet } from "react-native";

export default StyleSheet.create({
  todosContainer: {
    flex: 1,
    width: "100%",
    borderColor: "#000",
    borderWidth: 2,
    borderRadius: 15,
  },
});
